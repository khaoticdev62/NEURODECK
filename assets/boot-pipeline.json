{
  "$schema": "./boot-pipeline.schema.json",
  "version": "1.3.0",
  "description": "Boot pipeline manifest for NEURODECK cinematic startup. Add custom steps here to inject boot log entries without modifying Rust or JS source.",
  "steps": []
}
