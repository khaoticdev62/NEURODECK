# Story 2.1: Implement Steam Input mappings

Status: done

## Story

As a Steam Deck User,
I want terminal shortcuts mapped to Steam Input,
so that I can use the terminal efficiently with controller buttons.

## Acceptance Criteria

1. Define a set of standard shortcuts (e.g., Ctrl+S, Ctrl+E, Enter) mapped to specific controller inputs or action sets.
2. Provide a configuration file or guide for setting up Steam Input (e.g., a community layout or a JSON file).

## Tasks / Subtasks

- [x] Identify critical terminal shortcuts needed for daily use (AC: 1)
- [x] Create a Steam Input configuration or a guide on how to map keys (AC: 2)

## Dev Notes

- This is mostly configuration and documentation rather than Go code, unless we want to embed default mappings.
- We should check if we can export a Steam Input configuration file.

## Dev Agent Record
### Agent Model Used
Antigravity
