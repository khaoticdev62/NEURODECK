# Story 7.4: Create Example Automation Scripts

Status: done

## Story

As a Developer,
I want to create example Lua automation scripts,
so that users can learn how to use the scripting system.

## Acceptance Criteria

1. Create a few example Lua scripts in a `scripts` folder.
2. The scripts should demonstrate using `execute` and `print`.
3. Document the scripts and how to use them.

## Tasks / Subtasks

- [x] Create `scripts` directory if it doesn't exist.
- [x] Create `scripts/hello.lua` (simple print).
- [x] Create `scripts/system_info.lua` (uses execute to get OS info).
- [x] Create `scripts/cleanup.lua` (demonstrates file operations or cleanup).
- [x] Update documentation.

## Dev Notes

- These scripts serve as documentation and quick start guides.
- We can read them from file and execute them or just have them as reference.

## Dev Agent Record
### Agent Model Used
Antigravity
