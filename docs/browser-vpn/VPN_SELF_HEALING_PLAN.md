# VPN Self-Healing Plan

- Retry failed verification at most once immediately.
- Limit repeated recovery attempts per profile.
- Reapply browser proxy when the proxy layer drops.
- Treat unsupported or privilege-blocked states as terminal until the user changes the environment.

