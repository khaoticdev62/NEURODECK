# Fallow Terminal Audit Report

This report is generated by the terminal readiness script after the baseline and final verification passes.

It should list:

- unused terminal files
- dead UI paths
- duplicate command policy logic
- unsafe renderer imports
- mocked production terminal code

